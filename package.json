{
  "name": "@fluidapi/monaco-json-path-actions",
  "version": "0.2.0",
  "description": "Extensible Monaco editor actions for copying JSON paths from JSON/JSONC documents.",
  "type": "module",
  "license": "MIT",
  "author": "Fluid API",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/fluidapi/monaco-json-path-actions.git"
  },
  "keywords": [
    "monaco-editor",
    "json",
    "jsonc",
    "json-path"
  ],
  "sideEffects": false,
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && npm run build --workspaces --if-present",
    "build:core": "tsc -p tsconfig.build.json",
    "build:addons": "npm run build --workspaces --if-present",
    "test": "node --test test/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit && npm run typecheck --workspaces --if-present",
    "validate": "npm run typecheck && npm run build && npm test"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5"
  }
}
